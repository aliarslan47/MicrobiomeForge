// Bu makine (WSL2, 16 çekirdek / 12 GB RAM) için kaynak tavanları.
// taxprofiler 1.2.4 --max_memory'i yok sayıyor (yeni nf-core resourceLimits şeması),
// bu yüzden tavanlar burada process.resourceLimits ile verilir.
process {
    resourceLimits = [ cpus: 14, memory: 10.GB, time: 6.h ]
}
